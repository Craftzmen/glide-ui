import React from 'react'
import { Box } from '@chakra-ui/react';

const App = () => {
  return (
    <Box display={'inline-block'} m={10} fontSize={'sm'} bgColor={'gray.50'} textColor={'gray.700'} px={6} py={2} rounded={'lg'} >
      Glide - UI
    </Box>
  );
}

export default App;
